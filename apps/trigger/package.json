{
  "dependencies": {
    "@app/agents": "workspace:*",
    "@app/config": "workspace:*",
    "@app/db": "workspace:*",
    "@app/http": "workspace:*",
    "@opentelemetry/api": "catalog:",
    "@opentelemetry/sdk-node": "catalog:",
    "@sentry/node": "catalog:",
    "@sentry/opentelemetry": "catalog:",
    "@trigger.dev/sdk": "catalog:",
    "ai": "catalog:",
    "import-in-the-middle": "catalog:",
    "zod": "catalog:"
  },
  "description": "Trigger.dev workflows for the application",
  "devDependencies": {
    "trigger.dev": "catalog:",
    "@babel/preset-typescript": "^7.28.5",
    "@plexaris/eslint-config": "workspace:*",
    "@sentry/esbuild-plugin": "^4.9.1",
    "@trigger.dev/build": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "src/index.ts",
  "name": "@app/trigger",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules dist",
    "deploy": "bunx trigger.dev deploy --skip-update-check",
    "dev": "bunx trigger.dev dev --env-file ../../.env --skip-update-check",
    "fix": "eslint --fix --quiet --ignore-pattern='.trigger' . && prettier --ignore-path='../../.prettierignore' --log-level=silent --write .",
    "lint": "eslint --ignore-pattern='.trigger' . && prettier --ignore-path='../../.prettierignore' --check .",
    "typecheck": "tsc --noEmit"
  },
  "type": "module",
  "version": "0.0.0"
}
