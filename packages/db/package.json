{
  "name": "@app/db",
  "type": "module",
  "version": "0.0.1",
  "description": "Drizzle ORM database package for web2 app",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema.ts"
  },
  "scripts": {
    "clean": "rm -rf node_modules dist",
    "db:generate": "bun --env-file=../../.env drizzle-kit generate",
    "db:migrate": "bun --env-file=../../.env run src/migrate.ts",
    "db:studio": "bun --env-file=../../.env drizzle-kit studio",
    "db:push": "bun --env-file=../../.env drizzle-kit push",
    "db:drop": "bun --env-file=../../.env drizzle-kit drop",
    "typecheck": "tsc --noEmit",
    "lint": "eslint --ignore-pattern='./migrations/' . && prettier --ignore-path='../../.prettierignore' --check .",
    "fix": "eslint --fix --quiet --ignore-pattern='./migrations/' . && prettier --ignore-path='../../.prettierignore' --log-level=silent --write ."
  },
  "dependencies": {
    "drizzle-orm": "catalog:",
    "postgres": "^3.4.5"
  },
  "devDependencies": {
    "@kyoto/eslint-config": "workspace:*",
    "@types/node": "catalog:",
    "drizzle-kit": "^0.31.8",
    "tsconfig": "workspace:*",
    "typescript": "catalog:"
  }
}
