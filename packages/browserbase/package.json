{
  "name": "@app/browserbase",
  "type": "module",
  "version": "0.0.1",
  "description": "Browserbase tools for AI agents",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "clean": "rm -rf node_modules dist",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . && prettier --ignore-path='../../.prettierignore' --check .",
    "fix": "eslint --fix --quiet . && prettier --ignore-path='../../.prettierignore' --log-level=silent --write ."
  },
  "dependencies": {
    "@browserbasehq/sdk": "catalog:",
    "@browserbasehq/stagehand": "catalog:",
    "ai": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@kyoto/eslint-config": "workspace:*",
    "@types/node": "catalog:",
    "tsconfig": "workspace:*",
    "typescript": "catalog:"
  }
}
